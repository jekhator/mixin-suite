"""Constants for the decorators module.

Submodules expose constants via per-feature modules (e.g., sensitive, classes)
imported with the const alias:
  from mixin_sensitivity.decorators.constants.sensitive import … as const
"""
